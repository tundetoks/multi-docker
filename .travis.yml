sudo: required
services:
  - docker

before_install:
  - docker build -t tundetoks/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true tundetoks/react-test npm test

after_success:
  - docker build -t tundetoks/multi-client ./client
  - docker build -t tundetoks/multi-nginx ./nginx
  - docker build -t tundetoks/multi-server ./server
  - docker build -t tundetoks/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push tundetoks/multi-client
  - docker push tundetoks/multi-nginx
  - docker push tundetoks/multi-server
  - docker push tundetoks/multi-worker
